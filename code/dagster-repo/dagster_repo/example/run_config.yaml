ops:
  download_dataset:
    config:
      url: "https://archive.ics.uci.edu/ml/machine-learning-databases/wine-quality/winequality-white.csv"
  split_data:
    config:
      test_size: 0.2
  train_rfc:
    config:
      n_estimators: 50
      max_depth: 10
